# 守护桌面项目规则

> 本文件是项目通用规则，所有任务实施前必读，确保风格一致、避免重复踩坑。
> 本规则参考亲戚记忆APP（qinqilu）的成熟构建流程制定。

## 项目概述

- 名称：守护桌面（Guardian Launcher）
- 仓库：https://github.com/xueni97/guardian-launcher.git（main 分支）
- 定位：老人专用 Android 桌面启动器，大字体、一键拨号、阻止应用下载安装
- 用户：Android 手机，APK 侧载自用，无服务器、无后端

## 技术栈

| 层 | 技术 | 版本约束 |
|---|---|---|
| 框架 | Vue 3 + Vite 8 | ESM，`type: module` |
| UI | Vant 4 | 移动端组件库，全局放大字号 |
| 路由 | vue-router 4 | hash 模式（WebView 友好） |
| 本地存储 | localStorage | 联系人/设置（老人手机无需复杂 DB） |
| Android 打包 | Capacitor 8 | 网页资源内置 APK |
| 原生能力 | 自定义 Capacitor 插件 | 拨号/打开应用/设备管理员 |

## 目录结构约定

```
src/
├─ views/          # 页面组件（HomePage/ContactsPage/SettingsPage）
├─ db/             # 本地存储（contacts）
├─ utils/          # 工具函数（guardian 原生桥接 / web 降级）
├─ styles/         # 全局样式
└─ main.js         # 入口
android/
├─ app/src/main/java/com/xueni97/guardian/
│  ├─ MainActivity.java              # 入口，注册插件
│  ├─ GuardianPlugin.java            # 自定义 Capacitor 插件（核心）
│  └─ GuardianDeviceAdminReceiver.java  # 设备管理员接收器
├─ app/src/main/
│  ├─ AndroidManifest.xml            # HOME 主页 + CALL_PHONE + 设备管理员
│  └─ res/xml/device_admin.xml       # 设备管理员策略
.github/workflows/
└─ android-apk.yml    # APK 构建
```

## 编码规范

### 通用
- 所有代码注释和回复用中文
- 文件用 LF 换行，UTF-8 编码
- Vue 组件用 `<script setup>` 语法
- 函数和变量名用英文驼峰，UI 文案用中文

### 老人友好设计
- 根字号 `font-size: 22px`，按钮高度 72px
- 联系人头像 ≥ 88px，应用图标 ≥ 68px
- 高对比度配色，主色 `#1a73e8`
- 禁用文字选中（`user-select: none`），防止误触

## APK 构建规则

### 签名
- 使用固定 keystore：`android/app/guardian-release.keystore`
- 密码/别名硬编码在 `android/app/build.gradle` 的 `signingConfigs.release`
- CI 构建 `assembleRelease`（不是 debug），确保签名一致可覆盖安装
- **禁止**改用 debug 构建或更换 keystore

### CI 工作流
- 触发：push main（paths 过滤）或 workflow_dispatch
- 步骤：Node 24 → JDK 21 → npm ci → build → cap sync → gradlew assembleRelease
- `gradlew` 需在 CI 中 `chmod +x`（Windows 提交无执行权限位）
- Artifact 保留 90 天，命名 `guardian-v{run_number}.apk`

### 权限
- CALL_PHONE（一键直接呼叫）
- READ_PHONE_STATE
- INTERNET（Capacitor 必需）
- REQUEST_IGNORE_BATTERY_OPTIMIZATIONS（防止被系统杀）
- `<queries>` 声明要打开的应用包名（Android 11+ 包可见性）

### 设备所有者（Device Owner）
- 激活命令：`adb shell dpm set-device-owner com.xueni97.guardian/.GuardianDeviceAdminReceiver`
- 激活后可：阻止应用安装（DISALLOW_INSTALL_APPS）、隐藏应用、固定屏幕（Kiosk）
- **注意**：激活前需移除所有手机账户，且无法撤销（需恢复出厂设置解除）

## Git 提交规范

- 提交信息用中文，格式：`type: 简述`
  - `fix:` Bug 修复
  - `feat:` 新功能
  - `docs:` 文档更新
  - `refactor:` 重构
  - `chore:` 杂项
- PowerShell 不支持 heredoc，多行 commit message 用临时文件 `git commit -F file`（用完删除）
- 推送用 token URL：`git push https://xueni97:<token>@github.com/xueni97/guardian-launcher.git main`
- 不提交 dist/、node_modules/

## 常见坑与经验

1. **gradlew 权限**：Windows 提交的 gradlew 在 Linux CI 无执行权限，必须 `chmod +x`
2. **签名不一致**：debug 构建每次用随机密钥，必须用固定 release keystore
3. **状态栏重叠**：固定定位元素必须加 `env(safe-area-inset-top)`
4. **HOME 主页**：AndroidManifest 需同时声明 `HOME`、`DEFAULT`、`LAUNCHER` 三个 category
5. **包可见性**：Android 11+ 打开其他应用需在 `<queries>` 声明包名
6. **设备所有者**：`setApplicationHidden`、`addUserRestriction` 等 API 必须是 Device Owner 才能调用
7. **拨号权限**：`ACTION_CALL` 需 `CALL_PHONE` 运行时权限，无权限时降级为 `ACTION_DIAL`
8. **Capacitor sync 产物**：`android/app/src/main/assets/public/` 被 .gitignore 忽略，CI 里执行 sync 是正确做法

## 禁止事项

- 禁止改用 debug 构建或更换 keystore（会导致签名不一致无法覆盖安装）
- 禁止把 dist/ 或 cap sync 产物提交到仓库
- 禁止缩小字体或按钮尺寸（老人友好是核心需求）
- 禁止移除 `<queries>` 声明（Android 11+ 无法打开第三方应用）
